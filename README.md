# google-photos-takeout-metadata-fix
PowerShell script using ExifTool to fully restore missing metadata (Date/Time, GPS, Description) from Google Photos Takeout's JSON sidecar files back into the original images and videos (JPG, PNG, MP4, MOV). Solves the chronic metadata loss problem for Windows users.
